spring:
  cloud:
    config:
      uri: http://localhost:8200
  application:
    name: eureka-client
  profiles:
    active: peer1
server:
  port: 8300

management:
  endpoints:
    web:
      exposure:
        include: "*"
